/* ══════════════════════════════════════════════════════════════
   문의 · 가입 채널
   ──────────────────────────────────────────────────────────────
   실제 결제/가입 방식이 확정되지 않았으므로 결제 시스템을 임의로
   구현하지 않는다. 현재는 지점별 문의 채널로 연결하는 구조.

   채널이 확정되면 아래 값만 채우면 상담 시트와 모든 CTA에 반영된다.
   값이 null 이면 해당 버튼은 렌더링되지 않는다. (임의 생성 금지)
   ══════════════════════════════════════════════════════════════ */

export const GLOBAL_CONTACT = {
  /** 카카오톡 채널 URL — 예: 'http://pf.kakao.com/_xxxxxx' */
  kakaoUrl: null,
  /** 대표 전화 — 예: '055-000-0000' */
  phone: null,
  /** 온라인 신청 폼 URL (구글폼 / 자체 폼 등) */
  formUrl: null,
  /** 인스타그램 대표 계정 */
  instagramUrl: null,
}

/** 채널이 하나라도 열려 있는지 */
export const hasGlobalContact = Object.values(GLOBAL_CONTACT).some(Boolean)
